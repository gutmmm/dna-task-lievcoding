from __future__ import annotations


def answer_question(filename: str) -> str:
    """Run LLM for `question` using RAG.

    Parameters
    ----------
    filename
        file name
    question
        query question

    Returns
    -------
    result
        LLM response for `question`
    """

    result = ""

    return result
